<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.geekplus.java.oms.dao.ProductMapper">

    <select id="getProductList" resultType="Product">
        select id, name, quantity
        from product
    </select>

    <select id="getProductById" resultType="Product">
        select id, name, quantity
        from product
        where id = #{id}
    </select>

    <select id="getQuantity">
        select quantity
        from product
        where id = #{id}
    </select>

    <insert id="addProduct" keyProperty="id">
        insert into product (name, quantity)
        values(#{productName}, #{quantity})
    </insert>

    <update id="deductQuantity">
        update product
        set quantity = quantity - #{quantityToDeduct}
        where id = #{productId} and quantity >= #{quantityToDeduct}
    </update>

    <update id="setQuantity">
        update product
        set quantity = #{quantity}
        where id = #{productId}
    </update>

</mapper>